# Python-related
__pycache__/
*.pyc
*.pyd
.venv/  # Or your virtual environment directory name
venv/
env/
backend_venv/
fastapi_venv/  # Your specific virtual environment name (if applicable)
*.egg
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/

# OS-related
.DS_Store
Thumbs.db

# IDE-related (adjust as needed)
.idea/  # PyCharm
.vscode/ # VS Code
.spyproject/ # Spyder
*.swp  # Vim swap files
*~ # Emacs backup files

# Database-related (be careful with these!)
*.db  # SQLite databases (generally shouldn't be committed)
*.sqlite # SQLite databases (generally shouldn't be committed)
*.bak  # Database backups (be careful with these!)

# Logs and temporary files
*.log
*.tmp
temp/
tmp/
logs/

# Build and distribution
build/
dist/
*.whl
*.tar.gz
*.zip

# Secrets and configuration (VERY IMPORTANT!)
.env  # Environment variables (NEVER commit these!)
config.ini  # Or any other configuration files with sensitive data
secrets.json # Or any other file containing sensitive data

# Testing
.pytest_cache/
.coverage

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# Other (customize as needed)
.cache/
.history
__MACOSX/  # macOS specific

# If you're using Docker
.dockerignore

# If you're using Kubernetes
.kube/

# If you have a front-end build process (e.g., npm, yarn)
node_modules/
.npmrc
yarn.lock

# Add any other project-specific files or directories you don't want to track
hudddle-project-firebase.json